# dependencies
node_modules/

# secrets
.env.local
.env*.local

# build output
.next/
dist/
out/

# playwright / test artifacts
playwright-report/
test-results/
*.log

# cloned target repos live inside PR memory — never track them
.autoqa/**/repo/

# sample target app is its own git repo (a test target the agent clones)
/sample-app/

# Claude Code local state / transient worktrees
.claude/worktrees/

# OS
.DS_Store
